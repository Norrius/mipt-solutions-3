#ifndef SUFFIXARRAY_H
#define SUFFIXARRAY_H
#include <vector>
#include <string>
#include <unordered_map>
#include "external/SuffixTree.cpp"

std::vector<int> suffixArray(const std::string &string);

#endif // SUFFIXARRAY_H
